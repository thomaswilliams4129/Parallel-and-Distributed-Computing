Instructions how to run code:

1. Login onto the hpc lab

2. Upload the mpi_array_lab.c to the super computer

3. To run perform the following\
	a. Make sure the c file is in the director you are in
	b. Type mpicc mpi_array_lab.c -o mpi_array_lab
	c. Next to run the executable that was just created type time mpirun -np numberOfProcess ./mpi_array_lab sizeOfArray

4. Then it should output the code
